# MongoDB Connection
MONGODB_URI=mongodb+srv://root:OM5efz85AL4SB4Ad@power.ax8gn87.mongodb.net/?appName=Power

# Si es true, al arrancar se eliminan colecciones que existan en la BD pero no correspondan a ningún modelo (ver models/appCollectionNames.ts). Usar con cuidado.
# MONGODB_DROP_UNUSED_COLLECTIONS=true

# Server
PORT=3000
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Duración del token de sesión (opcional). Por defecto ~10 años (segundos). Ejemplos: 31536000 (1 año), 365d, 90d
# JWT_EXPIRES_IN=365d

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=noel.ponce.gonzalez@gmail.com
EMAIL_PASS=osam pwxk watx ensq
EMAIL_FROM=noreply@powerliftingtracker.com

# URL pública del frontend (emails, CORS). En AWS usa https://tu-dominio.com — en producción no se asume localhost.
APP_URL=http://localhost:3000
# Orígenes extra para CORS (coma), p. ej. CloudFront o otro subdominio
# CORS_ORIGINS=https://app.tu-dominio.com
# En producción, permitir localhost solo si lo necesitas (pruebas)
# ALLOW_LOCALHOST_CORS=false
MOBILE_APP_SCHEME=powerliftingtracker

# Expo Push Notifications (genera en https://expo.dev/accounts/_/settings/access-tokens)
EXPO_ACCESS_TOKEN=